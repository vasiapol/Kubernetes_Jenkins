podTemplate(label: 'kubernetes.io/hostname=node1', 
    containers: [
        containerTemplate(
            name: 'golang',
            image: 'jenkinsci/slave',
            ttyEnabled: true,
            command: 'cat'
        )
    ]
) {
    node ('pod-golang') {

        stage 'Switch to Utility Container'
        container('golang') {

          sh ("go version")

        }
    }
}