// this guarantees the node will use this template
def jenkins/label = "mypod-${UUID.randomUUID().toString()}"
podTemplate(label:jenkins/label) {
    node(jenkins/label) {
        stage('Run shell') {
            sh 'echo hello world'
        }
    }
}