// this guarantees the node will use this template
def label = "jenkins/mypod-${UUID.randomUUID().toString()}"
podTemplate(label:label) {
    node(label) {
        stage('Run shell') {
            sh 'echo hello world'
        }
    }
}